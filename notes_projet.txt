hexdump -C test.pcap

revenir sur ethernet et voir quels ethertype parser

icmp +ipv4 + ethernet

tcp + ipv4 + ethernet

arp + ethernet

ethernet + ipv4 + tcp / udp + dns

ethernet + ipv4 + tcp / udp + dhcp

ethernet + ipv4 + tcp / udp + http

ethernet + ipv4 + tcp / udp + ftp 


ARP : 

http://www.iana.org/assignments/arp-parameters/arp-parameters.xhtml

Mettre headers dans ethernet - IP à la place de "data"

DNS -> implémenter A, NS, MX

définition flux tcp -> numéros de séquence - ports
	mais on peut se servir de l'ip si c'est plus simple
	
fragmentation IP

Options à parser DHCP :

	Gateway (3)
	Ip Forwarding (19)
	Non local source routing(20)

Discover:
	Client identifier (67) ?
	Requested IP (50) ?

Request: 
	Message type (53)
	Hostname (12)

ACK :
	Subnet mask (1)
	Domain name server (6)
	Ip address lease time (51)


